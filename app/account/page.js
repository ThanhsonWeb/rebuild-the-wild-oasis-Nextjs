import SideNavigation from "../_components/SideNavigation";

export const metadata = {
	title: "Guest area",
};

function page() {
	return (
		<div>
			<SideNavigation />
		</div>
	);
}

export default page;
